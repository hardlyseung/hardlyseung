# 광주 ON AIR — 작업 규칙

AI 코딩 도구가 이 저장소에서 작업할 때 지켜야 할 규칙이다.
사람이 매번 같은 맥락을 다시 설명하지 않기 위해, 프로젝트의 판단 기준을 여기에 고정한다.

## 이 프로젝트가 무엇인가

광주관광공사 개방데이터를 현장에서 쓸 수 있는 형태로 재구성한 웹서비스.
문화관광해설 거점 18곳을 지도에서 찾고, 공식 자료에 근거한 AI 해설을 듣고, 현장 해설사로 연결된다.

공모전 출품작이며 **2027-03-31까지 외부 접속이 가능해야 한다.** 이 제약이 기술 선택을 지배한다.

## 절대 하지 말 것

1. **데이터에 없는 사실을 지어내지 않는다.**
   `places.json`의 해설 내용은 AI 답변의 유일한 근거다. 연도·인물·운영시간을 추측해서 채우면
   그 오류가 그대로 사용자에게 사실로 전달된다. 모르면 `TODO` 로 남긴다.

2. **`TODO` 로 시작하는 값을 사용자나 모델에게 노출하지 않는다.**
   화면 표시와 프롬프트 조립 양쪽에서 `isFilled()` 로 걸러야 한다.

3. **5·18 관련 서술은 공식 자료 외 출처를 쓰지 않는다.**
   국립5.18민주묘지, 5.18자유공원 두 곳이 해당한다.

4. **서버용 키를 커밋하지 않는다.**
   카카오 REST API 키, `GEMINI_API_KEY` 는 환경변수로만 다룬다.
   카카오 JavaScript 키는 클라이언트 공개 전제이므로 예외이며, 도메인 제한으로 보호한다.

5. **만료되는 무료 자원에 의존하지 않는다.**
   GCP 체험판 크레딧처럼 기한이 있는 자원은 2027-03-31 운영 조건을 위반한다.

6. **빌드 도구·프레임워크를 도입하지 않는다.**
   작성자가 프레임워크 미경험이며, 남은 기간에 학습 비용을 감당할 수 없다.
   순수 HTML/CSS/JS + 서버리스 함수 1개 구조를 유지한다.

## 설계 원칙

**하나가 죽어도 서비스는 산다.**
외부 의존성은 각각 독립적으로 실패할 수 있고, 실패해도 나머지는 동작해야 한다.

| 실패 | 대응 |
|---|---|
| 카카오 SDK 로드 실패 | 지도 자리에 원인 안내, 목록·상세·AI는 정상 |
| AI 실패·한도 초과·안전필터 차단 | 규칙 기반 빠른 안내는 정상 |
| 전시 데이터 미변환 | 해당 영역만 숨김 |
| 좌표 없는 거점 | 목록에 표시, 길찾기는 주소 검색으로 대체 |

새 기능을 붙일 때도 이 원칙을 지킨다. 초기화 실패가 `main()` 전체를 중단시키면 안 된다.

**데이터는 선언적으로.**
활용 데이터셋 목록, 검증 기록, 테마는 코드가 아니라 `places.json`에 선언한다.
데이터셋 추가가 코드 수정이 아니라 데이터 편집이어야 한다.

**정직하게 표시한다.**
건수는 실제 배열에서 센다. 레코드가 0인 데이터셋은 "활용했다"고 적지 않는다.

## 파일 구조

```
index.html          화면 구조
css/style.css       스타일 (디자인 토큰 · 다크모드 · 반응형)
js/app.js           지도·필터·목록·상세·전시·AI 추천·AI 호출
api/chat.js         서버리스 함수 — 근거 조립 + Gemini 호출 + 차단/오류 처리
vercel.json         보안 헤더 (빌드 설정은 없다)
data/places.json    거점 18곳 + 데이터셋 선언 + 검증 기록
data/exhibitions.json  전시·행사 개최 기록 (변환 전에는 빈 배열)
scripts/geocode.js             주소 → 좌표 (카카오 로컬 API)
scripts/convert-exhibitions.js 전시·행사 CSV → JSON (--facility 로 시설 지정)
tests/origin.js     /api/chat 출처 검증 점검 (의존성 없음)
tests/security.js   보안 방어 자동 점검 (의존성 없음)
tests/data-shape.js 데이터 내용 점검 — 파싱은 되는데 내용이 빈 경우를 잡는다
tests/dom-ids.js    app.js 가 찾는 DOM id 가 index.html 에 있는지 대조
.github/workflows/verify.yml  푸시·PR 마다 위 검증을 전부 실행
.vercelignore       프로덕션에 올리지 않을 것 (tests·scripts·docs·문서·죽은 파일)
README.md           프로젝트 소개 · 실행 · 배포 · 알려진 한계
SECURITY.md         위협 모델 · 무엇을 막고 무엇을 못 막는지
```

## 코드 관례

- 한국어 주석. 무엇을 하는지가 아니라 **왜 그렇게 했는지**를 적는다.
- DOM은 `el()` 헬퍼로 만든다. 데이터를 `innerHTML` 문자열에 끼워 넣지 않는다.
  예외 없다. 카카오 지도 팝업도 `CustomOverlay` 가 DOM 노드를 그대로 받으므로
  문자열 조립이 필요 없다 — 이스케이프를 빠뜨릴 자리를 아예 만들지 않는다.
- 사용자에게 보이는 오류 메시지는 **무엇이 잘못됐고 무엇을 하면 되는지**를 한국어로 말한다.
  "알 수 없는 오류" 같은 문구를 남기지 않는다.
- 외부 호출에는 타임아웃과 중복 실행 방지를 붙인다.
- 업스트림 오류 원문은 로그에만 남기고 브라우저로 보내지 않는다.

## 검증

이 저장소에는 테스트 프레임워크가 없다. 아래는 전부 의존성 없이 바로 돈다.

```bash
node --check js/app.js
node --check api/chat.js
node -e "require('./data/places.json'); require('./data/exhibitions.json')"

node tests/data-shape.js   # 데이터가 화면이 기대하는 모양인지
node tests/dom-ids.js      # app.js 가 찾는 id 가 index.html 에 있는지
node tests/origin.js       # /api/chat 출처 검증 15건
node tests/security.js     # 인젝션·비밀 차단·헤더 21건
```

**푸시하면 `.github/workflows/verify.yml` 이 위를 그대로 돌린다.**
이 저장소는 푸시가 곧 배포라, 여기서 못 막은 실수는 사용자 화면으로 나간다.
특히 데이터 갱신 절차가 GitHub 웹 편집기로 `data/places.json` 을 고치는 것인데
웹 편집기는 JSON 문법을 검사하지 않는다 — 쉼표 하나를 잡아낼 곳이 CI뿐이다.

DOM id 대조는 `tests/dom-ids.js` 가 대신한다. 다만 id 를 변수로 넘기는 호출은
정적으로 따라갈 수 없어, `app.js` 에 그런 헬퍼를 새로 만들면 그 스크립트의
`IDIRECT` 목록에 이름을 추가한다. 빠뜨리면 "확인 못 한 호출"로 보고된다.

실제 화면·지도·AI 응답은 배포된 주소에서 확인해야 한다. CI는 브라우저를 띄우지 않는다.
